name: mobile
description: "SCAN3D MOBILE — Metric 3D Scanner"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  archive: ^4.0.0
  uuid: ^4.2.0
  intl: ^0.19.0
  provider: ^6.1.0
  path: ^1.9.0
  sqflite_common_ffi: ^2.3.0
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
